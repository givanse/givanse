
```
{
  addToList(newItem) {
    this.list.push(newItem);
    this.list = list;
  }
}
```

```
  // data down, actions up
  // pass actions down to child components
```

```
  // cant' update an array inside an object
  // the array has to be outside
```

