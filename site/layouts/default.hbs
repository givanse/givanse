<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="language" content="english">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1"/>
    <meta name="author" content="Gastón I. Silva">
    <meta name="title" content="{{page.title}}"/>
    <meta name="description" content="{{page.description}}"/>
    <meta name="keywords" content="{{page.keywords}}"/>

    <meta property="og:type" content="article">
    <meta property="og:url" content="https://givan.se{{page.url}}/">
    <meta property="og:title" content="{{page.title}}">
    <meta property="og:site_name" content="{{site.title}}">
    <meta property="og:description" content="{{page.description}}">
    <meta property="og:image" content="https://givan.se{{page.thumb}}">

    <meta name="twitter:card" content="summary"/>
    <meta name="twitter:creator" content="@givanse"/>
    <meta name="twitter:title" content="{{page.title}}"/>
    <meta name="twitter:description" content="{{page.description}}"/>
    <meta name="twitter:url" content="https://givan.se{{page.url}}/"/>
    <meta name="twitter:image" content="https://givan.se{{page.thumb}}"/>

    <title>
      {{#if page.title}}
        {{page.title}}
      {{else}}
        {{site.title}}
      {{/if}}
    </title>
    <link rel="shortcut icon" type="image/ico" href="/favicon.ico"/>

    <link href="/vendor.css" rel="stylesheet" />
    <link href="/app.css" rel="stylesheet" />
    {{css-tags}}

    <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-47511141-1', 'auto');
      ga('send', 'pageview');
    </script>
  </head>

  <body>
    <header class="header container">
      <div class="row header_0">
        <nav class="row_max_w menu">
          {{#if page.index}}
            {{#link-to 'index' class='active'}}posts{{/link-to}}
          {{else}}
            {{#link-to 'index'}}posts{{/link-to}}
          {{/if}}

          {{#if page.about}}
            {{#link-to 'about' class='active'}}about{{/link-to}}
          {{else}}
            {{#link-to 'about'}}about{{/link-to}}
          {{/if}}
        </nav>
      </div>
      <div class="row header_1"></div>
      <div class="row header_2"></div>
    </header>
    <br>

    <div class="layout">
      <div class="container">
        {{{yield}}}
      </div>
     
      <div class="sticky_footer_push"></div>
    </div>

    <div class="sticky_footer"></div>

    {{javascript-tags}}
  </body>

</html>
